# Main More Wild entry

Native `UIMainView.RefreshMoreWild` 23baa28 reads the live MoreWild balance. A positive balance hides MoreWildWord (+138), activates MoreWildProgress (+120), formats `{0}/{1}` (GOT 4f1d9c8) with balance and live GetMoreWild, then assigns Image.fillAmount = float(balance)/float(config). The Image setter supplies its normal clamp. At zero or below it activates the word and hides the progress object without clearing the stored text/fill. `MoreWildRefresh` 23baa24 tail-calls this method; OnBeforeShow calls it at 23bb22c. The reconstructed component subscribes to the existing MoreWildChanged event and refreshes on bind.

Main OnClickButton 23bdce4..23bddac compares `MoreWild` (GOT 4f1e300), requests click sound, and opens UIMoreWildView with boxed false. There is no busy or balance guard in this branch. SDK handling is unchanged. The existing common sound event still requires its broader playback integration.

Source button is GO1855482821919830 / Rect224534964404139401, with standard Button and original EmptyRaycastGraphic as the target. It retains its visual children. Its parent Tubiao Rect224587083680832752 is (1080,499.17), centered at (-.003418,322.62) under the full-stretch main Content; the button is top-left anchored at (0,0), size (200,205), pivot (0,1). The authored MoreWildEntry retains that parent rectangle and only the relevant child. Tubiao is outside the Bottom/Main vs Bottom/Free containers changed by SetInitShow 23bca34, so it is not hidden by mode switching.

Original `ef_morewildicon.skel.bytes` is fully consumed (34526 bytes), idle duration 2 seconds, 39 timelines, two meshes. `Tools/Evidence/ef_morewildicon.json` preserves the source hash, atlas, bones, setup attachments and animation data. `Tools/prepare_more_wild_entry.py` flattens mesh influences for the existing native Unity UI rig author. `BuildMoreWildEntry.Save` imports it into the playfield; `BuildSpinPlayfield.Save` also attaches it on subsequent prefab rebuilds. Large atlas loading remains Resources-based. No original plugin assembly is added.

Integration validation extends the actual first-Spin/Extra-Wild test with real EventSystem hits on the main entry, paid popup opening, positive/zero balance visibility, fill ratio, mode visibility, next-Spin consumption, entry opening while Spin is busy, and GM rebinding. Latest visual: `Artifacts/current-more-wild-entry.png`. Results recorded after execution.

Final Unity 2022.3.62f3 PlayMode regression: `Artifacts/more-wild-entry-regression.xml`, **379/379 passed**, process 33740 exited. The first focused run reached GM teardown but the fixture read GameEntry.PlayerProgress after Select deliberately nulled it; corrected to retain the old progress object before Select and verify that its event no longer updates the discarded entry. No runtime fallback was added. The current capture was inspected: left-side native animated Wild button with visible `2/5` progress above Treasure.

## Source Node safe-area parent

BuildMoreWildEntry now retains Node (224901155395665742) above Tubiao (224587083680832752) and maps its original Adapt component to RecoveredScreenAdapt. Previously extraction started at Tubiao and lost the safety insets. SaveEntry supports reauthoring the hierarchy using the already recovered icon artwork; Save still builds that artwork before calling SaveEntry. The full SpinPlayfield author continues to call Attach.

The existing real-scene MoreWild test now checks Node adaptation with a 1080x1920 safe area inset by 24 on both sides, 60 at bottom and 120 at top. It raycasts the shifted entry and opens the actual window, then restores the real screen safe area and completes ad failure/reward, paid Spin decrement and GM teardown checks. Author PID 37924 exited successfully. Focused PlayMode PID 42272 terminated with 7/7 passing in 6.8809046 seconds (Artifacts/morewild-entry-adapt-tests.xml). The fresh current-more-wild-entry.png was inspected and shows the shifted entry. This does not prove whole-Main safe-area parity or physical-notch-device parity; other extracted branches remain to be checked. SDK handling is unchanged.
