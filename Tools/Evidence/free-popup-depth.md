# Free-session entry and exit popup ordering

The native ARM OnInitProperty methods UIFreeSpinStart 23b1458 and UIFreeSpinEndView 23afcdc both load the same eight-byte literal at dbb670 into UIWindowProperty +0x10/+0x14: (300, 2), Popup with Black mask. See core-popup-groups.md for the ELF decoding and UIManager group/depth evidence. BaseWindow versus PopupWindow changes their transition behavior, not their shared window group.

The recovered FreeEntryFlow and FreeExitFlow previously displayed their child windows at fixed depth 300. The actual core-loop regression now opens More Spin after the paid Spin starts, keeps it open through the Free session, and asserts both Free windows open above it. Before the fix, Artifacts/free-popup-depth-before.xml failed 0/1: the intro was 300 while More Spin was 301. Unity PID 1648 exited.

Both flow prefabs are now authored beneath CoreRoundFlow's PopupRoot, preserving their nested prefab ownership and serialized window references. Their show boundaries notify CoreRoundFlow to run the same active-group depth adjustment before activating the window. Unbind removes these subscriptions. The introductory popup scale transition, immediate Free-end hide, reward counting, and separate return-transition callbacks remain the existing recovered implementations.

The core-loop test now uses EventSystem raycasts for the Free intro's plain button and the final Continue button, instead of directly invoking those two UnityEvents. It also retains its existing checks for paid-spin consumption, Free generation, busy-state lifetime, task progress, audio, board shake, return-to-Base, and final persistence. Separately owned Bonus and ball-game windows still need their group integration audited; this change does not establish whole-game visual parity.

Validation: author PID 31460 exited successfully. The first fixed run (free-popup-depth-fixed.xml, PID 18012 exited) passed 5/6; the new raycast had no hits because the formerly nonvisual core fixture did not configure its portrait render target. The fixture now assigns a 1080x1920 target in sceneLoaded, matching the existing Bank UI fixture. No production layout workaround was added. Artifacts/free-popup-depth-portrait.xml passed 6/6 in 22.5425717 seconds, PID 31488 exited: core round, repeated Free rounds, all four ball routes, popup depth isolation, standalone Free entry, and standalone Free exit. The fresh Artifacts/current-free-start-popup-depth.png was inspected and shows the Free intro in front of More Spin; the underlying windows remain visible behind its dark mask. This is a current-project capture, not a fresh original-APK comparison.
